# Rust RESP API

a small project to learn REST APIs in rust, based on the book "zero two
production"

## How to run

For testing you need to run docker so sqlx can perform compile time verfication,
use the included script

```bash
./init_db.sh
```
