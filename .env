DB_USER=postgres
DB_PASSWORD=password
DB_PORT=5432
DB_HOST=localhost
DB_NAME=zero2prod-db

# this is for sqlx compile-time query verification, needs to match with DB values above
DATABASE_URL=postgres://postgres:password@localhost:5432/zero2prod-db
